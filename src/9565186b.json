import numpy as np 

def solve(t):
  arr = np.array(t)
  unique,count = np.unique(arr, return_counts=True)
  a = dict(zip(unique,count))
  b = max(a, key = a.get)
  for lst in arr:
    for j,lst_element in enumerate(lst):
      if (lst_element != b):
        lst[j] = 5
  print(arr)
